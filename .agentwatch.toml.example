[paths]
warn = [
  "**/.env*",
  "**/*auth*",
  "**/*secret*",
  "**/*token*",
  "**/*credential*",
  "**/*migration*",
]

deny = [
  "**/*private_key*",
  "**/*.pem",
  "**/*.key",
]

ignore = [
  ".git/**",
  ".agentwatch/**",
  "target/**",
  "node_modules/**",
  ".next/**",
]

[commands]
warn = [
  "git reset --hard",
  "git clean",
  "docker system prune",
  "drop database",
  "truncate table",
]

deny = [
  "rm -rf /",
  "rm -rf /*",
  "format c:",
]

# Approval Gate settings for supported AgentWatch-controlled providers.
# Warn decisions require human confirmation while enabled.
[approvals]
enabled = true
timeout_seconds = 600
